# Frontend

See the root [README](../README.md) for install and run instructions.

This Vite app contains placeholder pages only. Replace page markup from UI screenshots later; keep using `src/services/api.ts` and `src/types`.
